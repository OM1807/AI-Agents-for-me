{
  "name": "devorbit-ai",
  "version": "1.0.0",
  "private": true,
  "description": "DevOrbit AI - FastAPI + Next.js Monorepo with Claude Integration",
  "scripts": {
    "dev": "concurrently \"pnpm run dev:api\" \"pnpm run dev:web\"",
    "dev:web": "pnpm --filter @devorbit-ai/web dev",
   "dev:api": "cd apps/api && make run HOST=0.0.0.0 PORT=8000 RELOAD=1",
    "build": "turbo run build",
    "build:web": "pnpm --filter @devorbit-ai/web build",
    "build:ci": "cd apps/web && npm ci && npm run build",
    "test": "turbo run test",
    "test:api": "cd apps/api && make test",
    "test:api:cov": "cd apps/api && make test",
    "test:web": "pnpm --filter @devorbit-ai/web test",
    "format:api": "cd apps/api && make format",
    "lint": "turbo run lint",
    "lint:api": "cd apps/api && make lint",
    "lint:web": "pnpm --filter @devorbit-ai/web lint",
    "type-check": "turbo run type-check",
    "type-check:web": "pnpm --filter @devorbit-ai/web type-check",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build",
    "docker:logs": "docker-compose logs -f",
    "docker:logs:api": "docker-compose logs -f api",
    "docker:logs:web": "docker-compose logs -f web",
    "setup:env": "cp .env.example .env && echo 'Environment file created! Please edit .env to configure your settings.'",
    "setup": "pnpm install && pnpm run install:api && pnpm run setup:env"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "turbo": "^1.10.0"
  },
  "packageManager": "pnpm@8.6.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0",
    "python": ">=3.11.0"
  },
  "keywords": [
    "fastapi",
    "nextjs",
    "monorepo",
    "claude",
    "ai",
    "streaming",
    "typescript",
    "python",
    "poetry"
  ],
  "author": "DevOrbit AI Team",
  "license": "MIT"
}
